<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Itau_kt" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="android:windowSplashScreenBackground">@color/primary</item>
    </style>

    <style name="Theme.Itau_kt" parent="Base.Theme.Itau_kt" />
    <style name="inputLineBottom" parent="Widget.MaterialComponents.TextInputLayout.FilledBox.Dense">
        <item name="boxStrokeColor">@drawable/bg_input</item>
        <item name="boxStrokeWidth">2dp</item>
        <item name="startIconTint">@color/white</item>
    </style>
    <style name="Base.CardView.Help" parent="Base.CardView">
        <item name="cornerRadius">@dimen/five_dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">@dimen/one_seventy_hundred_dp</item>
        <item name="layout_constraintEnd_toEndOf">parent</item>
        <item name="layout_constraintStart_toStartOf">parent</item>
    </style>
</resources>